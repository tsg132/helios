#include "helios/policy_eval_op_neon.h"

// All methods are defined inline in the header.
// This compilation unit anchors the vtable for PolicyEvalOpNeon
// and ensures <arm_neon.h> compiles correctly in the build.

namespace helios {
}  // namespace helios
